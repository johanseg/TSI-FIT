Start development servers

Start both API and Worker in development mode:
1. Run `cd api && npm run dev` for the Express API on port 4900
2. Run `cd worker && npm run dev` for the BullMQ background worker

These need to run in separate terminals/processes.
