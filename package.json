{
  "name": "tsi-fit-score",
  "version": "1.0.0",
  "description": "TSI Fit Score Engine MVP - Lead enrichment and scoring system",
  "private": true,
  "workspaces": [
    "api",
    "worker",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "jest --config tests/jest.config.js"
  },
  "engines": {
    "node": ">=18.0.0 <23.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}

